<Page
    x:Class="KinoApp.Views.FavoritePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KinoApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:KinoApp.Controls"
    mc:Ignorable="d" NavigationCacheMode="Required"
    Style="{StaticResource PageStyle}">
    <Grid Margin="{StaticResource MediumLeftRightMargin}">
        <controls:MovieList Movies="{x:Bind ViewModel.Movies}" LoadMoreCommand="{x:Bind ViewModel.LoadMoreCommand}" 
                            Title="Избранные фильмы" OpenMovieCommand="{x:Bind ViewModel.OpenMovieCommand}"/>
    </Grid>
</Page>
